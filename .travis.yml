language: node_js
node_js:
  - "stable"
cache:
  directories:
  - front-end/node_modules
before_install:
  - cd front-end
script:
  - npm run test
  - npm run build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: master